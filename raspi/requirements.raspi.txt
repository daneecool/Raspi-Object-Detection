# Raspberry Pi 3B v1.2 Requirements
# Optimized for ARM64 architecture with 1GB RAM

# Core ML/Vision packages (use piwheels when possible)
ultralytics>=8.0.0,<9.0.0
opencv-python>=4.5.0,<5.0.0
numpy>=1.21.0,<2.0.0
Pillow>=8.0.0,<11.0.0

# System monitoring (essential for Pi)
psutil>=5.8.0

# Lightweight packages only
requests>=2.25.0
PyYAML>=5.4.0

# For testing framework
unittest-xml-reporting>=3.2.0

# Optional: ONNX for optimization (only if enough RAM)
# onnxruntime>=1.12.0,<2.0.0